"""pfbuild — phonefast Python build tool (shared modules).

Unifies the former build.sh + download-ocr-models.sh into a single Python
toolchain with a shared platform matrix, asset preparation, and build
orchestration. Standard library only (no third-party deps).
"""
